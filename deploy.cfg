[buildout]
extends = development.cfg
parts +=
    nginxconf

[nginxconf]
recipe = collective.recipe.template
input = etc/${nginxconf:domain}.nginx.conf.in
output = etc/${nginxconf:domain}.nginx.conf
domain = reinout.vanrees.org
